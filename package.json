{
  "name": "direct-demand-portal",
  "private": true,
  "scripts": {
    "dev:web": "cd frontend && pnpm dev",
    "build:web": "cd frontend && pnpm build",
    "dev:api": "cd backend && uv run uvicorn app.main:app --reload --port 8000",
    "test:api": "cd backend && uv run pytest"
  }
}
